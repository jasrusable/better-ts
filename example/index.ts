const test = iff(eql("hello", "yo"), "hi");

const blue = eql("hi", "yo");

// const names = map(
//   filter(["Jason", "Test", 1], (i) => boolean(i)),
//   (i) => 3
// );

// const t = thread(
//   ["Jason", "Nicol"],
//   filter((i) => eql("Jason", i)),
//   map((i) => `${i} Russell`)
// );
